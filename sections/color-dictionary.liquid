{% comment %} 
Create a section with blocks.

Each block will have a color field and text field. The text field will be the placeholder for the color name.

So when you loop your variants you take the color field value and apply it to the swatch. 
{% endcomment %}
{% style %}
    :root {
      {% for color_def in section.blocks %}
        --{{ color_def.settings.title | handleize }}: {{ color_def.settings.color_value }};
      {% endfor %}
    }
{% endstyle %}


{% schema %}
{
    "blocks": [
        {
            "name": "Color Definition",
            "type": "color-def",
            "settings": [
                {
                    "type": "color",
                    "id": "color_value",
                    "label": "Color Value",
                    "default": "#000000"
                },
                {
                    "type": "text",
                    "id": "title",
                    "label": "Color Name",
                    "default": "Color"
                }
            ]
        }
    ]
}
{% endschema %}